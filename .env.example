# Akili — Environment Variables
# Copy this file to .env and fill in your values.


# LLM Provider API keys (set the one matching providers.default in your config)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Optional: OpenAI API key (required when providers.default = "openai")
# OPENAI_API_KEY=sk-your-key-here

# Optional: Google Gemini API key (required when providers.default = "gemini")
# GEMINI_API_KEY=your-key-here

# Optional: Override PostgreSQL DSN (docker-compose sets this automatically)
# AKILI_DB_DSN=postgres://akili:akili@localhost:5432/akili?sslmode=disable

# Optional: HTTP API key mappings (format: key:userid,key2:userid2)
# AKILI_API_KEYS=dev-key:dev-user

# Optional: Slack gateway
# SLACK_SIGNING_SECRET=
# SLACK_BOT_TOKEN=

# Optional: Telegram gateway
# TELEGRAM_BOT_TOKEN=

# Optional: WhatsApp Business API (notification sender)
# WHATSAPP_ACCESS_TOKEN=

# Optional: Signal notification sender (via signal-cli-rest-api)
# SIGNAL_API_URL=http://localhost:8080
# SIGNAL_SENDER_NUMBER=+1234567890

# Optional: Signal gateway (via signal-cli-rest-api)
# SIGNAL_GATEWAY_API_URL=http://localhost:8080
# SIGNAL_GATEWAY_SENDER_NUMBER=+1234567890

# Optional: Override data directory (default: ~/.akili/data)
# AKILI_DATA_DIR=
# AKILI_CONFIG=
# AKILI_CLI_USER=

# Optional: Override workspace directory (default: ~/.akili/workspace)
# AKILI_WORKSPACE=

# Optional: HashiCorp Vault (for vault:// credential references)
# VAULT_ADDR=http://localhost:8200
# VAULT_TOKEN=hvs.your-token-here
# VAULT_NAMESPACE=
